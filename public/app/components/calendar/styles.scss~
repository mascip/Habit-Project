.habit-results-calendar {
    // width: 0%;
    .table {
        width: 0%; // As small as it can
        // height: 0%;
        // border-radius: 6px; // TODO: does not work
        // border-spacing: 20px; 
        tr, th {
            height: 0px;
            margin: 0px;
            padding: 0px;
            text-align: center;
        }
        td {
            text-align: left;
        }
    }
    .monthName {
        // padding-left: 25px;
    }
    .day-num {
        display: block;
        font-size: .7em;
        padding-left: 2px;
        // width: 0%;
    }
    td {
        padding: 1px !important;
    }
    button {
        margin: 0px;
        font-size: 1em;
        padding: 3px 6px;
    }
    .thisIsToday {
        background-color: hsl(60, 100%, 80%);
    }
    button.disabled {
        opacity: 0.2;
        i{ opacity: 0; }
    }
}
