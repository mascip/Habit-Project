$color-main-content: white;

body {
    $main-background-color: hsl(0, 0, 98%);
    @include gradient-vertical($start-color: $main-background-color, $end-color: darken($main-background-color, 10%));
}
.nav-tabs {
    li {
        a, a:focus, a:hover {
            margin-right: 6px;
            box-shadow: 2px -1px 2px 0px #888888;
            border: 1px solid #D3D3D3; //$nav-tabs-link-color;
            color: darken($nav-tabs-active-link-hover-color, 25%);
        }
        &.active > a {
            &, &:hover, &:focus {
                background-color: $color-main-content;
                border-bottom-color: transparent;
            }
        }
    }
}
#main-area {
    background-color: $color-main-content;
    padding-top: 20px;
}
