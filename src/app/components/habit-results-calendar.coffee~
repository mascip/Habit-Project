'use strict'
app_name = "myApp"
app = angular.module "#{app_name}.habitResultsCalendar", []

app.directive 'habitResultsCalendar', () ->
    restrict: 'E'
    templateUrl: '<div>Hi there</div>'
    replace: true
    # compile: -> alert('COMPILED!')
    # link: (scope, element, attrs) -> alert('LINKED!')
